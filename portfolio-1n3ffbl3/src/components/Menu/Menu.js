import styled from 'styled-components';

const Menu = styled.div`
	width: 90vw;
	display: flex;
	flex-direction: row;
	height: 60px;
	max-height: 100%;
	min-height: unset;
	align-items: baseline;
`;

export default Menu;